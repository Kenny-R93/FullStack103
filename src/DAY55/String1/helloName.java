package DAY55.String1;

public class helloName {
    public String helloName(String name) {              // Method that takes in a string variable "name"
        return "Hello " + name + "!";           // returns "Hello " + the string variable "name" + "!"
    }
}
